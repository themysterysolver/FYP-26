### AST

- Main folder is `ast_analysis.py` which works perfectly.
- This works accoridngly with patch generation so no problem.
- check for `HumanEval/132` 
- To fault infomration as `type,vale,message`.
- This code checks synatical errors along with structural errors,qwencoder didn't make any structural errors.

> PS : I have an idea to just extract errored id's and then use it for running pipeline and fixing? what do u say!